#include <stdio.h>
int main() {
int type,hours;
scanf("%d %d",&type,&hours);
switch(type)  {
    case 1:
    printf("%d",hours*10);
    break;
    case 2:
    printf("%d",hours*20);
    break;
    default:
    printf("%d",hours*20);
    break;
} 
    return 0;
}
