#include <stdio.h>
int main() {
    int a,b,c,total;
    scanf("%d %d %d",&a,&b,&c);
    total=a+b+c;
    if(a>=35&&b>=35&&c>=35) {
        printf("Pass\n");
        printf("Total %d", total);
    } else {
        printf("Fail");
    }
    return 0;
}
