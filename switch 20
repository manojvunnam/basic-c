#include <stdio.h>
int main() {
    int category;
    scanf("%d",&category);
    switch(category){
        case 1:
        printf("%d",1200);
        break;
        case 2:
        printf("%d",800);
        break;
        case 3:
        printf("%d",500);
        break;
    }
    
    return 0;
}
